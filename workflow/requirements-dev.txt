# 开发依赖精确锁定（与 pyproject.toml [project.optional-dependencies.dev] 对应）
# 安装方式：pip install -r requirements-dev.txt
# 注：CI 优先使用此文件以保证环境一致；本地开发也可改用 pip install -e ".[dev]"

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
ruff==0.15.20